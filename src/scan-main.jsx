import React from 'react'
import ReactDOM from 'react-dom/client'
import ScanApp from './ScanApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ScanApp />
  </React.StrictMode>,
)
